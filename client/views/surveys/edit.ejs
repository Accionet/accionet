<!DOCTYPE html>
<html ng-app="accionet-web-app">

<head>
    <% include ../partials/head %>
        <title>Accionet</title>
</head>

<body ng-controller="surveyController">
    <% include ../partials/message_log.ejs %>
        <%= include ../partials/navbar.ejs %>
            <div id="page-wrapper" ng-init="initializeSurveys(null, ' <%= JSON.stringify(survey[0]) %>')">
                <!-- set the course. Note that it has to be a string, otherwise it throws an error -->
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header"> Editar Encuesta </h1>
                    </div>
                    <!-- /.col-lg-12 -->
                </div>

                <% locals.edit = true %>
                    <% include partials/upsert.ejs %>
            </div>
            <!-- /#page-wrapper -->

            <% include ../partials/scripts.ejs %>
                <% include partials/surveys_scripts.ejs %>



</body>

</html>
